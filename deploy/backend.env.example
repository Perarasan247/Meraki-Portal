# Copy to backend/.env on the VPS and fill in real values.
# NEVER commit this file with real secrets. chmod 600 after creating it.

SUPABASE_URL=https://ktnsbslqynaspfngsdoi.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_JWT_SECRET=your-jwt-secret

# Same-origin in the VPS setup; used by CORS as a belt-and-braces allow-list.
FRONTEND_ORIGIN=https://merakiailabs.com
